import Link from 'next/link';import {getProfiles,getDrivers,getDocuments,getVehicles,getRides,getAccountControls} from '@/services/data';import {accountControlAction,reviewDocumentAction} from '@/app/actions';export const dynamic='force-dynamic';
export default async function DriverDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const [profiles,drivers,docs,vehicles,rides,controls]=await Promise.all([getProfiles(),getDrivers(),getDocuments(),getVehicles(),getRides(),getAccountControls()]);const p=profiles.find((x:any)=>x.id===id),d=drivers.find((x:any)=>x.user_id===id),c=controls.find((x:any)=>x.user_id===id);if(!d)return <div>Chauffeur introuvable.</div>;return <div><Link href="/chauffeurs" className="text-blue-700">← Chauffeurs</Link><h1 className="text-3xl font-black mt-3">{p?.first_name} {p?.last_name}</h1><div className="grid lg:grid-cols-2 gap-5 mt-5"><section className="card p-5"><h2 className="font-black">Informations</h2><div className="text-sm mt-3 space-y-2"><p>Téléphone : {p?.phone||'—'}</p><p>E-mail : {p?.email||'—'}</p><p>Pays : {p?.country_code||'—'}</p><p>Disponibilité : {d.status}</p><p>Note : {d.rating}</p><p>Courses : {rides.filter((r:any)=>r.driver_id===id).length}</p><p>Véhicules : {vehicles.filter((v:any)=>v.driver_id===id).map((v:any)=>`${v.make||''} ${v.model||''} ${v.plate_number||''}`).join(' • ')||'—'}</p></div><form action={accountControlAction} className="mt-5 grid gap-2"><input type="hidden" name="userId" value={id}/><input type="hidden" name="role" value="driver"/><select className="input" name="status" defaultValue={c?.admin_status||'pending'}><option value="pending">En attente</option><option value="validated">Validé</option><option value="invalidated">Invalidé</option></select><select className="input" name="active" defaultValue={String(c?.is_active!==false)}><option value="true">Compte actif</option><option value="false">Compte désactivé</option></select><textarea className="input" name="reason" placeholder="Motif interne" defaultValue={c?.internal_reason||''}/><button className="btn">Mettre à jour le compte</button></form></section><section className="card p-5"><h2 className="font-black">Documents</h2><div className="space-y-3 mt-3">{docs.filter((x:any)=>x.driver_id===id).map((doc:any)=><div key={doc.id} className="border rounded-xl p-3"><div className="flex justify-between gap-3"><div><b>{doc.document_type}</b><div className="text-xs muted">{doc.file_name} • {doc.status}</div>{doc.rejection_reason&&<div className="text-xs text-red-700">{doc.rejection_reason}</div>}</div><a className="btn secondary" href={`/api/documents/${doc.id}`} target="_blank">Ouvrir</a></div><form action={reviewDocumentAction} className="flex gap-2 mt-3"><input type="hidden" name="id" value={doc.id}/><select className="input" name="status" defaultValue={doc.status==='approved'?'approved':'rejected'}><option value="approved">Valider</option><option value="rejected">Refuser</option></select><input className="input flex-1" name="reason" placeholder="Motif de refus"/><button className="btn">Appliquer</button></form></div>)}</div></section></div></div>}
