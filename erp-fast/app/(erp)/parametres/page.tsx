import {commissionAction,settingAction} from '@/app/actions';import {getCommissionRules,getDrivers,getSettings,getVehicles} from '@/services/data';export const dynamic='force-dynamic';
export default async function Settings(){const [rules,drivers,settings,vehicles]=await Promise.all([getCommissionRules(),getDrivers(),getSettings(),getVehicles()]);const types=[...new Set(vehicles.map((v:any)=>v.vehicle_type))];return <div><h1 className="text-3xl font-black">Paramètres généraux</h1><p className="muted mb-5">Zones, horaires et commissions versionnées.</p><div className="grid lg:grid-cols-2 gap-5"><section className="card p-5"><h2 className="font-black">Nouvelle règle de commission</h2><p className="text-xs muted mt-1">Une règle existante n’est jamais modifiée : chaque changement crée une nouvelle version.</p><form action={commissionAction} className="grid gap-3 mt-4"><select className="input" name="commission_type"><option value="percentage">Pourcentage</option><option value="fixed">Montant fixe</option></select><input className="input" type="number" step="0.01" name="value" placeholder="Valeur" required/><select className="input" name="scope_type"><option value="global">Globale</option><option value="driver">Chauffeur spécifique</option><option value="vehicle_category">Catégorie véhicule</option></select><select className="input" name="driver_id"><option value="">Aucun chauffeur spécifique</option>{drivers.map((d:any)=><option key={d.user_id} value={d.user_id}>{d.user_id.slice(0,8)}</option>)}</select><select className="input" name="vehicle_type"><option value="">Aucune catégorie spécifique</option>{types.map((t:any)=><option key={t} value={t}>{t}</option>)}</select><input className="input" name="currency" defaultValue="XAF"/><label className="text-xs font-bold">Début d’effet<input className="input w-full mt-1" name="valid_from" type="datetime-local" required/></label><label className="text-xs font-bold">Fin d’effet facultative<input className="input w-full mt-1" name="valid_until" type="datetime-local"/></label><button className="btn">Créer la nouvelle règle</button></form></section><section className="card p-5"><h2 className="font-black">Paramètres clé-valeur</h2><div className="space-y-3 mt-4">{settings.map((s:any)=><form action={settingAction} key={s.key} className="border rounded-xl p-3"><input type="hidden" name="key" value={s.key}/><b className="text-sm">{s.key}</b><textarea className="input w-full mt-2" name="value" defaultValue={JSON.stringify(s.value,null,2)}/><button className="btn secondary mt-2">Enregistrer</button></form>)}</div><form action={settingAction} className="border rounded-xl p-3 mt-4"><input className="input w-full" name="key" placeholder="Nouvelle clé" required/><textarea className="input w-full mt-2" name="value" placeholder='Valeur JSON ou texte' required/><button className="btn mt-2">Ajouter</button></form></section></div><section className="card mt-6 overflow-x-auto"><div className="p-5"><h2 className="font-black">Historique des règles de commission</h2></div><table className="table"><thead><tr><th>Début</th><th>Type</th><th>Valeur</th><th>Portée</th><th>Cible</th><th>Devise</th></tr></thead><tbody>{[...rules].sort((a:any,b:any)=>+new Date(b.valid_from)-+new Date(a.valid_from)).map((r:any)=><tr key={r.id}><td>{new Date(r.valid_from).toLocaleString('fr-FR')}</td><td>{r.commission_type}</td><td>{r.value}</td><td>{r.scope_type}</td><td>{r.driver_id||r.vehicle_type||'Toutes'}</td><td>{r.currency}</td></tr>)}</tbody></table></section></div>}
