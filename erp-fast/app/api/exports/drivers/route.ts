import * as XLSX from 'xlsx';import {getDrivers,getProfiles,getAccountControls} from '@/services/data';export const dynamic='force-dynamic';export async function GET(){const [drivers,profiles,controls]=await Promise.all([getDrivers(),getProfiles(),getAccountControls()]);const rows=drivers.map((d:any)=>{const p=profiles.find((x:any)=>x.id===d.user_id),c=controls.find((x:any)=>x.user_id===d.user_id);return {ID:d.user_id,Nom:`${p?.first_name||''} ${p?.last_name||''}`.trim(),Telephone:p?.phone||'',Email:p?.email||'',Disponibilite:d.status,Statut_admin:c?.admin_status||'pending',Compte_actif:c?.is_active!==false,Note:d.rating,Courses:d.total_rides}});const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows),'Chauffeurs');const out=XLSX.write(wb,{type:'buffer',bookType:'xlsx'});return new Response(new Uint8Array(out),{headers:{'Content-Type':'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','Content-Disposition':'attachment; filename="FAST-chauffeurs.xlsx"'}})}
