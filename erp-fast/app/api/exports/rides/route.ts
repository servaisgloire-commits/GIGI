import * as XLSX from 'xlsx';import {getRides,getProfiles} from '@/services/data';export const dynamic='force-dynamic';export async function GET(){const [rides,profiles]=await Promise.all([getRides(),getProfiles()]);const name=(id:string|null)=>{const p=profiles.find((x:any)=>x.id===id);return p?`${p.first_name||''} ${p.last_name||''}`.trim():id||''};const rows=rides.map((r:any)=>({ID:r.id,Date:r.created_at,Client:name(r.client_id),Chauffeur:name(r.driver_id),Depart:r.pickup_address,Destination:r.destination_address,Statut:r.status,Prix:r.final_price??r.agreed_price??r.estimated_price,Devise:r.currency}));const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows),'Courses');const out=XLSX.write(wb,{type:'buffer',bookType:'xlsx'});return new Response(new Uint8Array(out),{headers:{'Content-Type':'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','Content-Disposition':'attachment; filename="FAST-courses.xlsx"'}})}
